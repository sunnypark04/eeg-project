# Minimal debug configuration for a short HPC lab run.
# The DeFungi image files live outside the GitHub repository.
data_dir: /gpfs/projects/dsci410_510/Lab6/defungi

image_size: 64
batch_size: 32
num_workers: 2
val_fraction: 0.2
seed: 42

epochs: 5
learning_rate: 0.001

model_file: outputs/model.pt
metrics_file: outputs/train_metrics.txt
